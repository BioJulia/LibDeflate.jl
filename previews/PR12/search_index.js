var documenterSearchIndex = {"docs":
[{"category":"section","location":"reference/#Reference","page":"Reference","text":"","title":"Reference"},{"category":"type","location":"reference/#LibDeflate.Compressor","page":"Reference","text":"Compressor(compresslevel::UInt8=0x06)\n\nCreate an object which can compress using the DEFLATE algorithm. compresslevel can be from level 1 (fast) to 12 (slow), and defaults to 0x06. Level 0 means no compression.\n\nCreating this object allocates, so when compressing multiple blocks, keep the same compressor in memory rather than making one for each block. If the C library fails to allocate this object, an OutOfMemory error is thrown.\n\nwarning: Warning\nCompressor is not thread safe, and therefore should not be used by different tasks concurrently. Concurrent use may cause undefined behaviour.\n\nSee also: compress!, unsafe_compress!\n\nExamples:\n\njulia> compressor = Compressor();\n\njulia> data = b\"Hello, world!\";\n\njulia> out = zeros(UInt8, deflate_compress_bound(compressor, UInt(sizeof(data))));\n\njulia> n = compress!(compressor, out, data);\n\njulia> roundtrip = zeros(UInt8, sizeof(data));\n\njulia> decompress!(Decompressor(), roundtrip, view(out, 1:n), UInt(sizeof(data)));\n\njulia> roundtrip == data\ntrue\n\n\n\n\n\n","title":"LibDeflate.Compressor"},{"category":"type","location":"reference/#LibDeflate.Decompressor","page":"Reference","text":"Decompressor()\n\nCreate an object which can decompress using the DEFLATE algorithm.\n\nCreating this object allocates, so when decompressing multiple blocks, keep the same decompressor in memory rather than making one for each block. If the C library fails to allocate this object, an OutOfMemory error is thrown.\n\nwarning: Warning\nDecompressor is not thread safe, and therefore should not be used by different tasks concurrently. Concurrent use may cause undefined behaviour.\n\nSee also: decompress!, unsafe_decompress!\n\nExamples:\n\njulia> decompressor = Decompressor();\n\njulia> compressed =\n       b\"\\x01\\x0d\\0\\xf2\\xff\\x48\\x65\\x6c\\x6c\\x6f\\x2c\\x20\\x77\\x6f\\x72\\x6c\\x64\\x21\";\n\njulia> out = zeros(UInt8, 13);\n\njulia> decompress!(decompressor, out, compressed);\n\njulia> String(out)\n\"Hello, world!\"\n\n\n\n\n\n","title":"LibDeflate.Decompressor"},{"category":"type","location":"reference/#LibDeflate.GzipDecompressAllResult","page":"Reference","text":"GzipDecompressAllResult\n\nResult of successfully decompressing one or more complete gzip members, i.e. a gzip file composed of multiple, concatenated gzip data.\n\nGzipDecompressAllResult is currently a NamedTuple, but may be changed to a named struct in a future release. The type name and properties are part of the public API; users should refer to this type as GzipDecompressAllResult instead of the NamedTuple subtype.\n\nIt has the following properties:\n\nread::UInt: total input bytes occupied by the completed members\nwritten::UInt: total decompressed bytes written by the completed members\nmembers::UInt: number of completed members\n\nExamples:\n\njulia> combined = vcat(\n           b\"\\x1f\\x8b\\x08\\0\\0\\0\\0\\0\\xff\\0\\x01\\x05\\0\\xfa\",\n           b\"\\xff\\x48\\x65\\x6c\\x6c\\x6f\\x82\\x89\\xd1\\xf7\\x05\\0\\0\\0\",\n           b\"\\x1f\\x8b\\x08\\0\\0\\0\\0\\0\\xff\\0\\x01\\x05\\0\\xfa\",\n           b\"\\xff\\x57\\x6f\\x72\\x6c\\x64\\x47\\x3e\\xb6\\xfb\\x05\\0\\0\\0\",\n       ); # gzip \"Hello\", then gzip \"World\"\n\njulia> out = zeros(UInt8, 10);\n\njulia> scratch = GzipDecompressAllScratch();\n\njulia> result = gzip_decompress_all!(decompressor, out, combined, scratch);\n\njulia> result.members === UInt(2)\ntrue\n\njulia> String(out)\n\"HelloWorld\"\n\n\n\n\n\n","title":"LibDeflate.GzipDecompressAllResult"},{"category":"type","location":"reference/#LibDeflate.GzipDecompressAllScratch","page":"Reference","text":"GzipDecompressAllScratch()\nGzipDecompressAllScratch(extra_fields, member_results)\n\nStorage  used by gzip_decompress_all! and unsafe_gzip_decompress_all!.\n\nIt has the following properties:\n\nextra_fields::Vector{GzipExtraField}: extra fields accumulated from every completed member.\nmember_results::Vector{GzipDecompressResult}: one result for every completed member.\n\nWhen a GzipDecompressAllScratch is passed to a function, both vectors are emptied before validation. On success, the scratch contains exactly the GzipExtraFields and GzipDecompressResults from all members. On error, it contains entries only from members completed before the failing member; the failing member contributes to neither vector.\n\n\n\n\n\n","title":"LibDeflate.GzipDecompressAllScratch"},{"category":"type","location":"reference/#LibDeflate.GzipDecompressResult","page":"Reference","text":"GzipDecompressResult\n\nResult of LibDeflate's gzip decompression.\n\nIt has the following fields:\n\nwritten::UInt number of decompressed bytes written\nread::UInt number of bytes read from input\nheader::GzipHeader metadata\n\nExamples:\n\njulia> compressed = vcat(\n           b\"\\x1f\\x8b\\x08\\0\\0\\0\\0\\0\\xff\\0\\x01\\x0d\\0\\xf2\\xff\\x48\\x65\\x6c\",\n           b\"\\x6c\\x6f\\x2c\\x20\\x77\\x6f\\x72\\x6c\\x64\\x21\\xe6\\xc6\\xe6\\xeb\\x0d\\0\\0\\0\",\n       ); # gzip \"Hello, world!\"\n\njulia> out = zeros(UInt8, 13);\n\njulia> fields = GzipExtraField[];\n\njulia> result = gzip_decompress!(decompressor, out, compressed, fields);\n\njulia> result.written === UInt(13)\ntrue\n\njulia> String(out)\n\"Hello, world!\"\n\n\n\n\n\n","title":"LibDeflate.GzipDecompressResult"},{"category":"type","location":"reference/#LibDeflate.GzipExtraField","page":"Reference","text":"GzipExtraField\n\nData structure for gzip extra data. Public properties:\n\ntag::NTuple{2, UInt8} two-byte tag\ndata::UnitRange{UInt} one-based location of subfield data in the original input. The range is empty when the subfield data has length zero.\n\nExamples:\n\njulia> header_bytes = b\"\\x1f\\x8b\\x08\\x04\\0\\0\\0\\0\\0\\xff\"; # FEXTRA flag set\n\njulia> extra_bytes = b\"\\x06\\0\\x41\\x42\\x02\\0\\x01\\x02\"; # XLEN, tag, len, data\n\njulia> bytes = vcat(header_bytes, extra_bytes);\n\njulia> fields = GzipExtraField[];\n\njulia> parse_gzip_header(bytes, fields);\n\njulia> fields[1].tag\n(0x41, 0x42)\n\njulia> bytes[fields[1].data] == b\"\\x01\\x02\"\ntrue\n\n\n\n\n\n","title":"LibDeflate.GzipExtraField"},{"category":"type","location":"reference/#LibDeflate.GzipHeader","page":"Reference","text":"GzipHeader\n\nStruct representing a gzip header. It has the following properties:\n\nmtime::Union{Nothing, NonZeroUInt32}: modification time of the file, as expressed by a UNIX timestamp mod 2^32. A zero timestamp means that the time is unavailable, and is represented by nothing; a nonzero timestamp is stored in a NonZeroUInt32.\nfilename::Union{Nothing, UnitRange{UInt}}: index of the filename in the header. nothing means that the FNAME flag is absent, while an empty range means that the flag is present but the filename is empty.\ncomment::Union{Nothing, UnitRange{UInt}}: index of the comment in the header. nothing means that the FCOMMENT flag is absent, while an empty range means that the flag is present but the comment is empty.\nextra::Union{Nothing, UnitRange{UInt}}: the indices of the gzip extra fields in the passed-in Vector{GzipExtraField} (or GzipDecompressAllScratch). Note that these scratch types are mutated when passed to a function, which invalidates this range. nothing means that the FEXTRA flag is absent, while an empty range means that the flag is present with XLEN == 0.\nflags::UInt8, extra_flags::UInt8, os::UInt8: the raw bytes from the stream.\n\nThe filename and comment ranges refer to raw bytes. Their encoding is not validated; RFC 1952 specifies ISO 8859-1 (Latin-1) for these fields.\n\nExamples:\n\njulia> header_bytes = b\"\\x1f\\x8b\\x08\\x08\\0\\0\\0\\0\\0\\xff\";\n\njulia> bytes = vcat(header_bytes, b\"hi\", b\"\\0\"); # gzip header, filename \"hi\"\n\njulia> fields = GzipExtraField[];\n\njulia> header = parse_gzip_header(bytes, fields).header;\n\njulia> String(bytes[header.filename])\n\"hi\"\n\n\n\n\n\n","title":"LibDeflate.GzipHeader"},{"category":"type","location":"reference/#LibDeflate.NonZeroUInt32","page":"Reference","text":"NonZeroUInt32\n\nContainer that stores an UInt32 value, which cannot be zero. Obtain the inner value with the .x property:\n\nExamples\n\njulia> x = NonZeroUInt32(UInt32(3)); x.x\n0x00000003\n\njulia> NonZeroUInt32(UInt32(0))\nERROR: ArgumentError: Cannot construct a NonZeroUInt32 from zero\n[...]\n\n\n\n\n\n","title":"LibDeflate.NonZeroUInt32"},{"category":"type","location":"reference/#LibDeflate.ReadableMemory","page":"Reference","text":"ReadableMemory\n\nStruct that wraps a pointer and a length. This struct is not garbage-collector aware, so must be used with GC.@preserve. This type can be constructed from DenseArray{UInt8}, String, SubString{String} and some SubArrays. It may also be directly constructed from a Ptr and an Integer. To make custom types available as input for LibDeflate, add a constructor taking your custom type.\n\nExamples:\n\njulia> v = [0x01, 0x02, 0x03, 0x04];\n\njulia> GC.@preserve v begin\n           r = ReadableMemory(v)\n           @assert sizeof(r) === 4\n           ptr = Ptr{UInt32}(pointer(r))\n           htol(unsafe_load(ptr)) |> show\n       end\n0x04030201\n\nSee also: WriteableMemory\n\n\n\n\n\n","title":"LibDeflate.ReadableMemory"},{"category":"type","location":"reference/#LibDeflate.WriteableMemory","page":"Reference","text":"WriteableMemory\n\nStruct that wraps a pointer and a length. This struct is not garbage-collector aware, so must be used with GC.@preserve. This type can be constructed from Vector{UInt8}, Memory{UInt8}, and some subtypes of SubArray. It may also be directly constructed from a Ptr and an Integer. To make custom types available as output for LibDeflate, add a constructor taking the custom type. This type implements pointer(::WriteableMemory)::Ptr{Nothing} and sizeof(::WriteableMemory)::Int.\n\nSee also: ReadableMemory\n\nExamples:\n\njulia> v = fill(0x00, 4);\n\njulia> GC.@preserve v begin\n           w = WriteableMemory(v)\n           @assert sizeof(w) === 4\n           ptr = Ptr{UInt32}(pointer(w))\n           unsafe_store!(ptr, 0x01020304)\n           unsafe_load(ptr) |> show\n       end\n0x01020304\n\n\n\n\n\n","title":"LibDeflate.WriteableMemory"},{"category":"function","location":"reference/#LibDeflate.adler32","page":"Reference","text":"adler32(data, start::UInt32=UInt32(1))::UInt32\n\nCalculate the Adler-32 checksum of data with seed start.\n\nReadableMemory(data) is constructed safely by preserving data from garbage collection for the duration of the call. Custom input types can opt in by implementing that constructor.\n\nSee also: unsafe_adler32\n\nExamples:\n\njulia> adler32(b\"hello world\")\n0x1a0b045d\n\njulia> adler32(b\" world\", adler32(b\"hello\")) == adler32(b\"hello world\")\ntrue\n\n\n\n\n\n","title":"LibDeflate.adler32"},{"category":"method","location":"reference/#LibDeflate.compress!-Tuple{Compressor, Any, Any}","page":"Reference","text":"compress!(::Compressor, output, input)::Union{LibDeflateError, UInt}\n\nCompress input as a DEFLATE payload into the beginning of output, returning the number of bytes written or LibDeflateErrors.deflate_insufficient_space if the output is too small. The output is never resized.\n\nOn error, return a LibDeflateError, and leave the content of output in an arbitrary state.\n\nReadableMemory(input) and WriteableMemory(output) are constructed safely by preserving both arguments from garbage collection for the duration of the call. Custom input and output types can opt in by implementing those constructors. This function does not check whether the input and output memory regions overlap (alias); the caller must ensure that they do not.\n\nSee also: unsafe_compress!\n\nExamples:\n\njulia> data = b\"Hello, world!\";\n\njulia> out = zeros(UInt8, deflate_compress_bound(compressor, UInt(sizeof(data))));\n\njulia> n = compress!(compressor, out, data);\n\njulia> roundtrip = zeros(UInt8, sizeof(data));\n\njulia> decompress!(decompressor, roundtrip, view(out, 1:n), UInt(sizeof(data)));\n\njulia> roundtrip == data\ntrue\n\n\n\n\n\n","title":"LibDeflate.compress!"},{"category":"function","location":"reference/#LibDeflate.crc32","page":"Reference","text":"crc32(data, start::UInt32=UInt32(0))::UInt32\n\nCalculate the CRC-32 checksum of data with seed start. Note that CRC-32 is a different and slower algorithm than the crc32c provided in the Julia standard library.\n\nReadableMemory(data) is constructed safely by preserving data from garbage collection for the duration of the call. Custom input types can opt in by implementing that constructor.\n\nSee also: unsafe_crc32\n\nExamples:\n\njulia> crc32(b\"hello world\")\n0x0d4a1185\n\njulia> crc32(b\" world\", crc32(b\"hello\")) == crc32(b\"hello world\")\ntrue\n\n\n\n\n\n","title":"LibDeflate.crc32"},{"category":"method","location":"reference/#LibDeflate.decompress!-Tuple{Decompressor, Any, Any, UInt64}","page":"Reference","text":"decompress!(\n    ::Decompressor, output, input, [n_out::UInt]\n)::Union{LibDeflateError, @NamedTuple{read::UInt, written::UInt}}\n\nDecompress a DEFLATE stream, reading from the beginning of input and writing decompressed data to the beginning of output. Reading stops at the end of the DEFLATE stream, so trailing input is left unread. On success, return the number of bytes read and written.\n\nOn error, return a LibDeflateError, and leave the content of output in an arbitrary state.\n\nThe function returns LibDeflateErrors.deflate_insufficient_space if the decompressed data does not fit. If the exact decompressed size is known, pass it as n_out to use the faster known-size path. An incorrect size returns LibDeflateErrors.deflate_output_too_short or LibDeflateErrors.deflate_insufficient_space.\n\nReadableMemory(input) and WriteableMemory(output) are constructed safely by preserving both arguments from garbage collection for the duration of the call. Custom input and output types can opt in by implementing those constructors. This function does not check whether the input and output memory regions overlap (alias); the caller must ensure that they do not.\n\nSee also: unsafe_decompress!\n\nExamples:\n\njulia> compressed =\n       b\"\\x01\\x0d\\0\\xf2\\xff\\x48\\x65\\x6c\\x6c\\x6f\\x2c\\x20\\x77\\x6f\\x72\\x6c\\x64\\x21\";\n\njulia> out = zeros(UInt8, 13);\n\njulia> decompress!(decompressor, out, compressed);\n\njulia> String(out)\n\"Hello, world!\"\n\n\n\n\n\n","title":"LibDeflate.decompress!"},{"category":"method","location":"reference/#LibDeflate.deflate_compress_bound-Tuple{Compressor, UInt64}","page":"Reference","text":"deflate_compress_bound(compressor::Compressor, input_size::UInt)::Union{LibDeflateError, UInt}\n\nReturn a worst-case upper bound on the number of bytes produced by compress! when compressing input_size bytes with compressor. This is generally slightly larger than input_size.\n\nThe bound may overestimate the required space, but an output buffer of this size is guaranteed to be sufficient. This calculation is constant-time with respect to input_size. Returns LibDeflateErrors.overflow if the bound cannot be represented as a UInt.\n\nExamples:\n\njulia> bound = deflate_compress_bound(compressor, UInt(1000));\n\njulia> bound >= 1000\ntrue\n\n\n\n\n\n","title":"LibDeflate.deflate_compress_bound"},{"category":"method","location":"reference/#LibDeflate.gzip_compress!-Tuple{Compressor, Any, Any}","page":"Reference","text":"gzip_compress!(\n    compressor::Compressor,\n    output,\n    input;\n    comment=nothing,\n    filename=nothing,\n    extra=nothing,\n    mtime::Union{NonZeroUInt32, Nothing} = nothing,\n    header_crc::Bool=false\n)::Union{LibDeflateError, UInt}\n\nCompress input as gzip data into the fixed-size buffer output, returning the number of bytes written or LibDeflateErrors.deflate_insufficient_space if it does not fit. The output is never resized.\n\nOn error, return a LibDeflateError, and leave the content of output in an arbitrary state.\n\nUse gzip_compress_bound to determine an output size that is guaranteed to be sufficient.\n\nOptional comment, filename, and extra metadata are omitted when set to nothing. comment and filename must not contain a zero byte. extra must represent valid gzip extra data and must not exceed typemax(UInt16) bytes. comment and filename are otherwise treated as uninterpreted bytes: their encoding is not validated, although RFC 1952 specifies ISO 8859-1 (Latin-1). Currently the OS byte is set to 255, XFL set to 2 (compression level 12), 4 (compression level 1), or 0 (any other level), according to RFC 1952 v4.2, and the FTEXT flag unset. This behaviour is subject to change.\n\nmtime is the modification time in seconds since the Unix epoch represented by a NonZeroUInt32, or nothing if not available.\n\nWriteableMemory(output) and ReadableMemory wrappers for input and every non-nothing metadata argument are constructed safely by preserving those arguments from garbage collection for the duration of the call. Custom input, output, and metadata types can opt in by implementing the corresponding constructors. This function does not check whether the output memory region overlaps (aliases) the input or a metadata region; the caller must ensure that it does not.\n\nSee also: unsafe_gzip_compress!\n\nExamples:\n\njulia> data = b\"Hello, world!\";\n\njulia> out = zeros(UInt8, gzip_compress_bound(compressor, UInt(sizeof(data))));\n\njulia> n = gzip_compress!(compressor, out, data);\n\njulia> roundtrip = zeros(UInt8, sizeof(data));\n\njulia> fields = GzipExtraField[];\n\njulia> gzip_decompress!(\n           decompressor, roundtrip, view(out, 1:n), UInt(sizeof(data)), fields,\n       );\n\njulia> roundtrip == data\ntrue\n\n\n\n\n\n","title":"LibDeflate.gzip_compress!"},{"category":"method","location":"reference/#LibDeflate.gzip_compress_bound-Tuple{Compressor, UInt64}","page":"Reference","text":"gzip_compress_bound(\n    compressor::Compressor,\n    input_size::UInt;\n    comment_len::Union{Nothing, UInt} = nothing,\n    filename_len::Union{Nothing, UInt} = nothing,\n    extra_len::Union{Nothing, UInt16} = nothing,\n    header_crc::Bool = false,\n)::Union{LibDeflateError, UInt}\n\nReturn a worst-case upper bound on the number of bytes produced by gzip_compress! with the given input and metadata sizes. A metadata length of nothing means that the corresponding optional field will not be present, while 0 represents a present but empty field.\n\nReturns a LibDeflateErrors.overflow if the result would overflow UInt.\n\nThe bound may overestimate the required space, but an output buffer of this size is guaranteed to be sufficient. This calculation is constant-time with respect to all supplied sizes.\n\nExamples:\n\njulia> bound = gzip_compress_bound(compressor, UInt(100));\n\njulia> bound_with_name = gzip_compress_bound(compressor, UInt(100); filename_len=UInt(8));\n\njulia> bound_with_name > bound\ntrue\n\n\n\n\n\n","title":"LibDeflate.gzip_compress_bound"},{"category":"method","location":"reference/#LibDeflate.gzip_decompress!-Tuple{Decompressor, Any, Any, Vector{GzipExtraField}}","page":"Reference","text":"gzip_decompress!(\n    ::Decompressor, output, input,\n    extra_fields::Vector{GzipExtraField}\n)::Union{GzipDecompressResult, LibDeflateError}\n\ngzip_decompress!(\n    ::Decompressor, output, input, n_out::UInt,\n    extra_fields::Vector{GzipExtraField}\n)::Union{GzipDecompressResult, LibDeflateError}\n\nDecompress the first gzip member in input into the fixed-size buffer output. Return LibDeflateErrors.deflate_insufficient_space if the decompressed data does not fit.\n\nIf the exact decompressed size is known, pass it as n_out to use the faster known-size decompression path. An incorrect size returns LibDeflateErrors.deflate_output_too_short or LibDeflateErrors.deflate_insufficient_space.\n\nOn success, the returned result reports both the decompressed length and the total number of input bytes consumed by that member. Following gzip members or trailing data are left unread.\n\nOn error, return a LibDeflateError, and leave the content of output in an arbitrary state.\n\nThe function empties extra_fields before validation and appends the member's parsed extra fields to it. The returned header's extra range contains the indices of those fields in extra_fields.\n\nReadableMemory(input) and WriteableMemory(output) are constructed safely by preserving both arguments from garbage collection for the duration of the call. Custom input and output types can opt in by implementing those constructors. This function does not check whether the input and output memory regions overlap (alias); the caller must ensure that they do not.\n\nSee also: unsafe_gzip_decompress!\n\nExamples:\n\njulia> compressed = vcat(\n           b\"\\x1f\\x8b\\x08\\0\\0\\0\\0\\0\\xff\\0\\x01\\x0d\\0\\xf2\\xff\\x48\\x65\\x6c\",\n           b\"\\x6c\\x6f\\x2c\\x20\\x77\\x6f\\x72\\x6c\\x64\\x21\\xe6\\xc6\\xe6\\xeb\\x0d\\0\\0\\0\",\n       ); # gzip \"Hello, world!\"\n\njulia> out = zeros(UInt8, 13);\n\njulia> fields = GzipExtraField[];\n\njulia> gzip_decompress!(decompressor, out, compressed, fields);\n\njulia> String(out)\n\"Hello, world!\"\n\n\n\n\n\n","title":"LibDeflate.gzip_decompress!"},{"category":"method","location":"reference/#LibDeflate.gzip_decompress_all!-Tuple{Decompressor, Any, Any, GzipDecompressAllScratch}","page":"Reference","text":"gzip_decompress_all!(\n    ::Decompressor, output, input, scratch::GzipDecompressAllScratch\n)::Union{\n    GzipDecompressAllResult,\n    Tuple{GzipDecompressAllResult, LibDeflateError},\n}\n\nDecompress every member of a gzip file in input into the fixed-size buffer output. The input must contain at least one complete member and no trailing data.\n\nOn success, return GzipDecompressAllResult with statistics of the decompression result. If an error is encountered, return a tuple containing the same statistics for all members completed before the error, followed by the LibDeflateError.\n\nOn error, return a tuple of result and a LibDeflateError. The first result.written bytes of output are well-defined and valid, but any subsequent bytes are in an arbitrary undefined state.\n\nEmpty both vectors in scratch before validation. On return, scratch.extra_fields contains fields from every completed member and scratch.member_results contains one result per completed member. A failing member contributes to neither vector.\n\nReadableMemory(input) and WriteableMemory(output) are constructed safely by preserving both arguments from garbage collection for the duration of the call. Custom input and output types can opt in by implementing those constructors. This function does not check whether the input and output memory regions overlap (alias); the caller must ensure that they do not.\n\nSee also: gzip_decompress!, unsafe_gzip_decompress_all!\n\nExamples:\n\njulia> combined = vcat(\n           b\"\\x1f\\x8b\\x08\\0\\0\\0\\0\\0\\xff\\0\\x01\\x05\\0\\xfa\",\n           b\"\\xff\\x48\\x65\\x6c\\x6c\\x6f\\x82\\x89\\xd1\\xf7\\x05\\0\\0\\0\",\n           b\"\\x1f\\x8b\\x08\\0\\0\\0\\0\\0\\xff\\0\\x01\\x05\\0\\xfa\",\n           b\"\\xff\\x57\\x6f\\x72\\x6c\\x64\\x47\\x3e\\xb6\\xfb\\x05\\0\\0\\0\",\n       ); # gzip \"Hello\", then gzip \"World\"\n\njulia> out = zeros(UInt8, 10);\n\njulia> scratch = GzipDecompressAllScratch();\n\njulia> result = gzip_decompress_all!(decompressor, out, combined, scratch);\n\njulia> result.members === UInt(2)\ntrue\n\njulia> String(out)\n\"HelloWorld\"\n\n\n\n\n\n","title":"LibDeflate.gzip_decompress_all!"},{"category":"method","location":"reference/#LibDeflate.is_valid_extra_data-Tuple{Any}","page":"Reference","text":"is_valid_extra_data(data)::Bool\n\nCheck whether data represents valid gzip metadata for the \"extra\" field. Gzip extra data cannot exceed typemax(UInt16) bytes.\n\nReadableMemory(data) is constructed safely by preserving data from garbage collection for the duration of the call. Custom input types can opt in by implementing that constructor.\n\nSee also: unsafe_is_valid_extra_data\n\nExamples:\n\njulia> is_valid_extra_data(b\"\\x41\\x42\\x02\\0\\x01\\x02\")\ntrue\n\njulia> is_valid_extra_data(b\"\\x41\\x42\\x02\\0\\x01\") # too short for declared length\nfalse\n\n\n\n\n\n","title":"LibDeflate.is_valid_extra_data"},{"category":"method","location":"reference/#LibDeflate.parse_gzip_header-Tuple{Any, Vector{GzipExtraField}}","page":"Reference","text":"parse_gzip_header(\n    input,\n    extra_fields::Vector{GzipExtraField}\n)::Union{\n    LibDeflateError, @NamedTuple{read::UInt, header::GzipHeader}\n}\n\nParse the input data, returning the number of bytes read and a GzipHeader, or a LibDeflateError. The parser empties extra_fields before validation, then appends every parsed gzip extra field to it. The returned header's extra range contains the indices of those fields in extra_fields.\n\nReadableMemory(input) is constructed safely by preserving input from garbage collection for the duration of the call. Custom input types can opt in by implementing that constructor.\n\nSee also: unsafe_parse_gzip_header\n\nExamples:\n\njulia> header_bytes = b\"\\x1f\\x8b\\x08\\x08\\0\\0\\0\\0\\0\\xff\";\n\njulia> bytes = vcat(header_bytes, b\"hi\", b\"\\0\"); # gzip header, filename \"hi\"\n\njulia> fields = GzipExtraField[];\n\njulia> result = parse_gzip_header(bytes, fields);\n\njulia> result.read === UInt(13)\ntrue\n\njulia> String(bytes[result.header.filename])\n\"hi\"\n\n\n\n\n\n","title":"LibDeflate.parse_gzip_header"},{"category":"function","location":"reference/#LibDeflate.unsafe_adler32","page":"Reference","text":"unsafe_adler32(in::ReadableMemory, start::UInt32=UInt32(1))::UInt32\n\nLow-level variant of adler32 that operates directly on ReadableMemory and has the same checksum behavior. The caller must keep the allocation referenced by in alive, typically by wrapping both construction of the memory wrapper and this call in GC.@preserve.\n\nSee also: adler32\n\n\n\n\n\n","title":"LibDeflate.unsafe_adler32"},{"category":"method","location":"reference/#LibDeflate.unsafe_compress!-Tuple{Compressor, WriteableMemory, ReadableMemory}","page":"Reference","text":"unsafe_compress!(\n    ::Compressor, out::WriteableMemory, in::ReadableMemory\n)::Union{UInt, LibDeflateError}\n\nLow-level variant of compress! that operates directly on WriteableMemory and ReadableMemory. It has the same compression behavior, return value, and errors as compress!.\n\nOn error, return a LibDeflateError, and leave the content of out in an arbitrary state.\n\nThe caller must keep the allocations referenced by out and in alive, typically by wrapping both construction of the memory wrappers and this call in GC.@preserve. The memory regions referenced by out and in must not overlap (alias).\n\nSee also: compress!\n\n\n\n\n\n","title":"LibDeflate.unsafe_compress!"},{"category":"function","location":"reference/#LibDeflate.unsafe_crc32","page":"Reference","text":"unsafe_crc32(in::ReadableMemory, start::UInt32=UInt32(0))::UInt32\n\nLow-level variant of crc32 that operates directly on ReadableMemory and has the same checksum behavior. The caller must keep the allocation referenced by in alive, typically by wrapping both construction of the memory wrapper and this call in GC.@preserve.\n\nSee also: crc32\n\n\n\n\n\n","title":"LibDeflate.unsafe_crc32"},{"category":"method","location":"reference/#LibDeflate.unsafe_decompress!-Tuple{Decompressor, WriteableMemory, ReadableMemory}","page":"Reference","text":"unsafe_decompress!(\n    ::Decompressor, output::WriteableMemory, input::ReadableMemory,\n    [n_out::UInt]\n)::Union{@NamedTuple{read::UInt, written::UInt}, LibDeflateError}\n\nLow-level variant of decompress! that operates directly on WriteableMemory and ReadableMemory. It has the same decompression behavior, return values, and errors as decompress!.\n\nOn error, return a LibDeflateError, and leave the content of output in an arbitrary state.\n\nThe caller must keep the allocations referenced by output and input alive, typically by wrapping both construction of the memory wrappers and this call in GC.@preserve. The memory regions referenced by output and input must not overlap (alias).\n\nSee also: decompress!\n\n\n\n\n\n","title":"LibDeflate.unsafe_decompress!"},{"category":"method","location":"reference/#LibDeflate.unsafe_gzip_compress!-Tuple{Compressor, WriteableMemory, ReadableMemory}","page":"Reference","text":"unsafe_gzip_compress!(\n    compressor::Compressor,\n    out::WriteableMemory,\n    in::ReadableMemory;\n    comment::Union{Nothing, ReadableMemory} = nothing,\n    filename::Union{Nothing, ReadableMemory} = nothing,\n    extra::Union{Nothing, ReadableMemory} = nothing,\n    mtime::Union{NonZeroUInt32, Nothing} = nothing,\n    header_crc::Bool=false,\n)::Union{LibDeflateError, UInt}\n\nLow-level variant of gzip_compress! that operates directly on WriteableMemory and ReadableMemory. It has the same compression behavior, metadata rules, return value, and errors as gzip_compress!.\n\nOn error, return a LibDeflateError, and leave the content of out in an arbitrary state.\n\nThe caller must keep the allocations referenced by out, in, and every non-nothing metadata argument alive, typically by wrapping both construction of the memory wrappers and this call in GC.@preserve. The memory region referenced by out must not overlap the regions referenced by in or any metadata argument.\n\nSee also: gzip_compress!\n\n\n\n\n\n","title":"LibDeflate.unsafe_gzip_compress!"},{"category":"method","location":"reference/#LibDeflate.unsafe_gzip_decompress!-Tuple{Decompressor, WriteableMemory, ReadableMemory, Vector{GzipExtraField}}","page":"Reference","text":"unsafe_gzip_decompress!(\n    ::Decompressor, output::WriteableMemory, input::ReadableMemory,\n    extra_fields::Vector{GzipExtraField}\n)::Union{LibDeflateError, GzipDecompressResult}\nunsafe_gzip_decompress!(\n    ::Decompressor, output::WriteableMemory, input::ReadableMemory, n_out::UInt,\n    extra_fields::Vector{GzipExtraField}\n)::Union{LibDeflateError, GzipDecompressResult}\n\nLow-level variant of gzip_decompress! that operates directly on WriteableMemory and ReadableMemory. It has the same decompression behavior, return value, and errors as gzip_decompress!.\n\nOn error, return a LibDeflateError, and leave the content of output in an arbitrary state.\n\nThe caller must keep the allocations referenced by output and input alive, typically by wrapping both construction of the memory wrappers and this call in GC.@preserve. The memory regions referenced by output and input must not overlap (alias).\n\nSee also: gzip_decompress!\n\n\n\n\n\n","title":"LibDeflate.unsafe_gzip_decompress!"},{"category":"method","location":"reference/#LibDeflate.unsafe_gzip_decompress_all!-Tuple{Decompressor, WriteableMemory, ReadableMemory, GzipDecompressAllScratch}","page":"Reference","text":"unsafe_gzip_decompress_all!(\n    ::Decompressor, output::WriteableMemory, input::ReadableMemory,\n    scratch::GzipDecompressAllScratch\n)::Union{\n    GzipDecompressAllResult,\n    Tuple{GzipDecompressAllResult, LibDeflateError},\n}\n\nLow-level variant of gzip_decompress_all! that operates directly on WriteableMemory and ReadableMemory. It has the same decompression behavior, return value, errors, and effects on scratch as gzip_decompress_all!.\n\nOn error, return a tuple of result and a LibDeflateError. The first result.written bytes of output are well-defined and valid, but any subsequent bytes are in an arbitrary undefined state.\n\nThe caller must keep the allocations referenced by output and input alive, typically by wrapping both construction of the memory wrappers and this call in GC.@preserve. The memory regions referenced by output and input must not overlap (alias).\n\nSee also: gzip_decompress_all!\n\n\n\n\n\n","title":"LibDeflate.unsafe_gzip_decompress_all!"},{"category":"method","location":"reference/#LibDeflate.unsafe_is_valid_extra_data-Tuple{ReadableMemory}","page":"Reference","text":"unsafe_is_valid_extra_data(data::ReadableMemory)::Bool\n\nLow-level variant of is_valid_extra_data that operates directly on ReadableMemory and has the same validation behavior. The caller must keep the allocation referenced by data alive, typically by wrapping both construction of the memory wrapper and this call in GC.@preserve.\n\nSee also: is_valid_extra_data\n\n\n\n\n\n","title":"LibDeflate.unsafe_is_valid_extra_data"},{"category":"method","location":"reference/#LibDeflate.unsafe_parse_gzip_header-Tuple{ReadableMemory, Vector{GzipExtraField}}","page":"Reference","text":"unsafe_parse_gzip_header(\n    input::ReadableMemory, extra_fields::Vector{GzipExtraField}\n)\n\nLow-level variant of parse_gzip_header that operates directly on ReadableMemory. It has the same parsing behavior, return value, and errors as parse_gzip_header.\n\nThe caller must keep the allocation referenced by input alive, typically by wrapping both construction of the memory wrapper and this call in GC.@preserve.\n\nSee also: parse_gzip_header\n\n\n\n\n\n","title":"LibDeflate.unsafe_parse_gzip_header"},{"category":"method","location":"reference/#LibDeflate.unsafe_zlib_compress!-Tuple{Compressor, WriteableMemory, ReadableMemory}","page":"Reference","text":"unsafe_zlib_compress!(\n    ::Compressor, output::WriteableMemory, input::ReadableMemory\n)::Union{LibDeflateError, UInt}\n\nLow-level variant of zlib_compress! that operates directly on WriteableMemory and ReadableMemory. It has the same compression behavior, return value, and errors as zlib_compress!.\n\nOn error, return a LibDeflateError, and leave the content of output in an arbitrary state.\n\nThe caller must keep the allocations referenced by output and input alive, typically by wrapping both construction of the memory wrappers and this call in GC.@preserve. The memory regions referenced by output and input must not overlap (alias).\n\nSee also: zlib_compress!\n\n\n\n\n\n","title":"LibDeflate.unsafe_zlib_compress!"},{"category":"method","location":"reference/#LibDeflate.unsafe_zlib_decompress!-Tuple{Decompressor, WriteableMemory, ReadableMemory}","page":"Reference","text":"unsafe_zlib_decompress!(\n    ::Decompressor, output::WriteableMemory, input::ReadableMemory,\n    [n_out::UInt]\n)::Union{LibDeflateError, UInt}\n\nLow-level variant of zlib_decompress! that operates directly on WriteableMemory and ReadableMemory. It has the same decompression behavior, return value, and errors as zlib_decompress!.\n\nOn error, return a LibDeflateError, and leave the content of output in an arbitrary state.\n\nThe caller must keep the allocations referenced by output and input alive, typically by wrapping both construction of the memory wrappers and this call in GC.@preserve. The memory regions referenced by output and input must not overlap (alias).\n\nSee also: zlib_decompress!\n\n\n\n\n\n","title":"LibDeflate.unsafe_zlib_decompress!"},{"category":"method","location":"reference/#LibDeflate.zlib_compress!-Tuple{Compressor, Any, Any}","page":"Reference","text":"zlib_compress!(::Compressor, output, input)::Union{LibDeflateError, UInt}\n\nCompress input as a zlib stream into output, returning the number of bytes written or a LibDeflateError if the output is too small. The output is never resized.\n\nOn error, return a LibDeflateError, and leave the content of output in an arbitrary state.\n\nReadableMemory(input) and WriteableMemory(output) are constructed safely by preserving both arguments from garbage collection for the duration of the call. Custom input and output types can opt in by implementing those constructors. This function does not check whether the input and output memory regions overlap (alias); the caller must ensure that they do not.\n\nSee also: unsafe_zlib_compress!\n\nExamples:\n\njulia> data = b\"Hello, world!\";\n\njulia> out = zeros(UInt8, zlib_compress_bound(compressor, UInt(sizeof(data))));\n\njulia> n = zlib_compress!(compressor, out, data);\n\njulia> roundtrip = zeros(UInt8, sizeof(data));\n\njulia> zlib_decompress!(\n           decompressor, roundtrip, view(out, 1:n), UInt(sizeof(data)),\n       );\n\njulia> roundtrip == data\ntrue\n\n\n\n\n\n","title":"LibDeflate.zlib_compress!"},{"category":"method","location":"reference/#LibDeflate.zlib_compress_bound-Tuple{Compressor, UInt64}","page":"Reference","text":"zlib_compress_bound(compressor::Compressor, input_size::UInt)::Union{LibDeflateError, UInt}\n\nReturn a worst-case upper bound on the number of bytes produced by zlib_compress! when compressing input_size bytes with compressor.\n\nThe bound may overestimate the required space, but an output buffer of this size is guaranteed to be sufficient. This calculation does not inspect any input data and is constant-time with respect to input_size. Returns LibDeflateErrors.overflow if the bound cannot be represented as a UInt.\n\nExamples:\n\njulia> bound = zlib_compress_bound(compressor, UInt(1000));\n\njulia> bound >= 1000\ntrue\n\n\n\n\n\n","title":"LibDeflate.zlib_compress_bound"},{"category":"method","location":"reference/#LibDeflate.zlib_decompress!-Tuple{Decompressor, Any, Any}","page":"Reference","text":"zlib_decompress!(\n    ::Decompressor, output, input, [n_out::UInt]\n)::Union{LibDeflateError, UInt}\n\nDecompress input as a zlib stream into output. If the exact decompressed size is known, pass it as n_out to use the faster known-size path. Return the number of bytes written or a LibDeflateError.\n\nOn error, return a LibDeflateError, and leave the content of output in an arbitrary state.\n\nReadableMemory(input) and WriteableMemory(output) are constructed safely by preserving both arguments from garbage collection for the duration of the call. Custom input and output types can opt in by implementing those constructors. This function does not check whether the input and output memory regions overlap (alias); the caller must ensure that they do not.\n\nSee also: unsafe_zlib_decompress!\n\nExamples:\n\njulia> compressed = vcat(\n           b\"\\x78\\x5e\\x01\\x0d\\0\\xf2\\xff\\x48\\x65\\x6c\\x6c\\x6f\",\n           b\"\\x2c\\x20\\x77\\x6f\\x72\\x6c\\x64\\x21\\x20\\x5e\\x04\\x8a\",\n       ); # zlib \"Hello, world!\"\n\njulia> out = zeros(UInt8, 13);\n\njulia> zlib_decompress!(decompressor, out, compressed);\n\njulia> String(out)\n\"Hello, world!\"\n\n\n\n\n\n","title":"LibDeflate.zlib_decompress!"},{"category":"module","location":"reference/#LibDeflate.LibDeflateErrors","page":"Reference","text":"Module LibDeflateErrors\n\nDummy module to contain the variants of the LibDeflateError enum.\n\n\n\n\n\n","title":"LibDeflate.LibDeflateErrors"},{"category":"type","location":"reference/#LibDeflate.LibDeflateErrors.LibDeflateError","page":"Reference","text":"LibDeflateError\n\nA UInt8 enum representing that LibDeflate encountered an error. The numerical value of the errors are not stable across non-breaking releases, but their names are. Code checking for specific errors should check by e.g. == LibDeflateErrors.gzip_not_deflate. Successful operations will not return a LibDeflateError.\n\nExamples:\n\njulia> c = vcat(\n           b\"\\x01\\x0d\\0\\xf2\\xff\\x48\\x65\\x6c\\x6c\",\n           b\"\\x6f\\x2c\\x20\\x77\\x6f\\x72\\x6c\\x64\\x21\",\n       );\n\njulia> out = zeros(UInt8, 2); # too small to hold the decompressed data\n\njulia> err = decompress!(decompressor, out, c);\n\njulia> err isa LibDeflateError\ntrue\n\njulia> err == LibDeflateErrors.deflate_insufficient_space\ntrue\n\n\n\n\n\n","title":"LibDeflate.LibDeflateErrors.LibDeflateError"},{"category":"section","location":"#LibDeflate.jl","page":"LibDeflate","text":"LibDeflate.jl provides high-performance, low-level APIs for compressing and decompressing gzip, zlib, and DEFLATE formats. It is much faster than other implementations, but only works with in-memory buffers; it cannot compress or decompress streamed data. Therefore, LibDeflate.jl is useful when compression or decompression is a bottleneck, but your data is small enough to fit in memory. Examples include blocked compression and situations where many small gzip packages are sent over a network.\n\nLibDeflate.jl implements the computationally lightweight parts in Julia and delegates the computationally intensive parts to Eric Biggers's C library, libdeflate.\n\nThe API is intended to be low-level and precisely documented; for example, abstract types are avoided and precise unsigned integer types are used. The APIs have low allocation overhead (but are not zero-allocation) and are trimmable.\n\nwarning: Warning\nLibDeflate.jl's APIs are generally not thread-safe or safe in the presence of aliasing between memory buffers passed to a single function. It is the user's responsibility to ensure that no Compressor or Decompressor is used concurrently and that no two arguments to a function call alias.\n\nMost APIs come in safe and unsafe variants. The unsafe ones take ReadableMemory and WriteableMemory, which are structs that simply hold a pointer and a length. These functions allow LibDeflate to be used with foreign (non-Julia-owned) memory. The safe variants construct these memory types internally from Julia-owned memory and call the unsafe ones. This incurs almost no overhead, so it is the preferred API when processing Julia-owned memory, such as a regular Vector{UInt8}. To use custom types with the safe functions, you must implement constructors for ReadableMemory(::MyType) or WriteableMemory(::MyType).\n\nSee the reference in the sidebar for the full API. For an overview, this package implements:\n\nSingle-member gzip de/compression\nMulti-member gzip decompression\nDEFLATE de/compression\nzlib de/compression\nFunctions for obtaining an upper bound on the number of bytes written when compressing a given number of bytes\nCRC-32\nAdler-32","title":"LibDeflate.jl"}]
}
